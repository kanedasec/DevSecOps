name: Full Security Pipeline

on:
  workflow_dispatch:

jobs:
  static-analysis:
    uses: ./.github/ci/static-analysis.yml

  trivy-scan:
    uses: ./.github/ci/trivy-scan.yml
    needs: static-analysis

  evaluate:
    uses: ./.github/ci/evaluate-results.yml
    needs: trivy-scan