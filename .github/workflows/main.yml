name: Full Security Pipeline

on:
  workflow_dispatch:

jobs:
  Secrets-Scan:
    uses: .github/workflows/Gitleaks-secret.yml@main

  static-analysis:
    uses: .github/workflows/static-analysis.yml@main

  trivy-scan:
    uses: .github/workflows/trivy-Container-scan.yml@main
    needs: static-analysis

  evaluate:
    uses: .github/workflows/evaluate-results.yml@main
    needs: trivy-scan
