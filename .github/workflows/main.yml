name: Full Security Pipeline

on:
  workflow_dispatch:

jobs:
  static-analysis:
    uses: kanedasec/DevSecOps/ci/static-analysis.yml

  trivy-scan:
    uses: kanedasec/DevSecOps/ci/trivy-scan.yml
    needs: static-analysis

  evaluate:
    uses: kanedasec/DevSecOps/ci/evaluate-results.yml
    needs: trivy-scan